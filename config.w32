// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("extensions", "for extensions support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("extensions", "enable extensions support", "no");

if (PHP_EXTENSIONS != "no") {
	EXTENSION("extensions", "extensions.c");
}

